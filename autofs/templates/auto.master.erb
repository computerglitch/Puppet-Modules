#This is an automounter map and it has the following format
# key [ -mount-options-separated-by-comma ] location
# For details of the format look at autofs(5).
#
<% @auto_master.each do |mount| -%>
<%= mount %> 
<% end -%>

/misc    /etc/auto.misc
/net     -hosts

+auto.master
